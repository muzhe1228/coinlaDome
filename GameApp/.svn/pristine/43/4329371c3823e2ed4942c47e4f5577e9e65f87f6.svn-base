import styled from "styled-components";
import $ from "common/minxin";

export const FengPanWrapper = styled.div`
  position: fixed;
  width: 100%;
  height: 100%;
  top: 0.659176rem;
  left: 0;
  background-color: ${$.bgAlpha("#131b29", "0.6")};
  z-index: 2;
  display: flex;
  justify-content: center;
  align-items: center;
  > img {
    position: absolute;
    right: 1.06367rem;
    bottom: 0.2397rem;
    width: 1.857678rem;
    height: 1.423221rem;
  }
`;
export const BeforStopWrapper = styled.div`
  color: #0e2647;
  padding-top: 0.449438rem;
  display: flex;
  align-items: center;
  p {
    text-align: center;
    &:last-child {
      font-size: ${$.size28};
      span {
        &:first-child {
          color: #d8d8d8;
          i {
            color: #d8d8d8;
            font-size: ${$.size28};
          }
        }
      }
    }
  }
`;
export const StopWrapper = styled.div`
  padding-top: 0.2397rem;
  font-size: ${$.size28};
  color: #0e2647;
  display: flex;
  align-items: center;
`;
