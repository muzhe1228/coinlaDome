import DataOwner from "./DataOwner";

// 用户pk信息

export default class UserPkInfo extends DataOwner {
    
    // 获取可用资产
    public getAvailableBalance() : number {
        return parseInt(this._data.availableBalance,10)
    }
}